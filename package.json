{
 "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run start --prefix frontend\" \"nodemon Backend/server.js\"",
    "build": "npm run build --prefix frontend",  
    "start": "node Backend/server.js"  
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
